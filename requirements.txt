# BON v13 — Facebook Publisher
# ──────────────────────────────────────────────────────────────────────────────
# Ce fichier installe UNIQUEMENT les dépendances obligatoires (core).
# Il est conservé pour compatibilité avec les outils qui lisent requirements.txt.
#
# ► Installation standard (usage publication seulement) :
#     pip install -r requirements.txt && playwright install chromium
#
# ► Installation avec toutes les options (API, scheduler, Excel, tests) :
#     pip install -r requirements.txt -r requirements-optional.txt
#     playwright install chromium
#
# ► Voir requirements-core.txt et requirements-optional.txt pour le détail.
# ──────────────────────────────────────────────────────────────────────────────

-r requirements-core.txt
